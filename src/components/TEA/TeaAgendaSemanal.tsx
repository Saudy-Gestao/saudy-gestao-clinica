import { useEffect, useMemo, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Badge, Box, Button, Divider, Group, Paper, Skeleton, Stack, Text, Tooltip } from '@mantine/core';
import { useElementSize, useMediaQuery } from '@mantine/hooks';
import { showNotification } from '@mantine/notifications';
import { ChevronLeft, ChevronRight, Clock3, Search } from 'lucide-react';
import dayjs from 'dayjs';
import 'dayjs/locale/pt-br';
import { Header } from '../Header/Header';
import { useTeaWeeklyAgendaQuery, type TeaAgendaItem } from '../../hooks/useTeaWeeklyAgendaQuery';
import { resolveApiErrorMessage } from '../../lib/apiError';
import { FloatingInput } from '../common/FloatingInput';
import { FloatingMultiSelect } from '../common/FloatingMultiSelect';
import { AGENDA_CARD_DENSITY, resolveAgendaCardPresentation, type AgendaCardDetailLevel } from '../../utils/agendaCardDensity';

const WEEKDAY_LABELS = ['Seg', 'Ter', 'Qua', 'Qui', 'Sex'];
const SLOTS = ['08:00', '08:45', '09:30', '10:15', '11:00', '11:45', '12:30', '13:15', '14:00', '14:45', '15:30', '16:15', '17:00', '17:45', '18:30', '19:15'];
const MOCK_PATIENTS = [
  ['Miguel Torres', 'Psicologia', 'Camila Duarte', 'Sala 1 (Unidade Central)', '08:00', 'CONFIRMED'], ['Helena Pires', 'Fonoaudiologia', 'Rafael Nogueira', 'Sala 2 (Unidade Central)', '09:30', 'SCHEDULED'], ['Davi Rocha', 'Terapia ocupacional', 'Beatriz Alves', 'Sala 3 (Unidade Central)', '11:00', 'CONFIRMED'], ['Alice Barros', 'Fisioterapia', 'Thiago Prado', 'Sala 4 (Unidade Central)', '14:00', 'RESERVED'], ['Bernardo Melo', 'Psicologia', 'Camila Duarte', 'Sala 1 (Unidade Central)', '15:30', 'SCHEDULED'], ['Sofia Ramos', 'Fonoaudiologia', 'Rafael Nogueira', 'Sala 2 (Unidade Sul)', '08:45', 'CONFIRMED'], ['Enzo Martins', 'Terapia ocupacional', 'Beatriz Alves', 'Sala 3 (Unidade Sul)', '10:15', 'CONFIRMED'], ['Laura Nunes', 'Fisioterapia', 'Thiago Prado', 'Sala 4 (Unidade Sul)', '16:15', 'PENDING'], ['Isabela Costa', 'Psicologia', 'Camila Duarte', 'Sala 1 (Unidade Central)', '09:30', 'CONFIRMED'], ['Gabriel Lima', 'Fonoaudiologia', 'Rafael Nogueira', 'Sala 2 (Unidade Central)', '13:15', 'SCHEDULED'], ['João Vitor', 'Terapia ocupacional', 'Beatriz Alves', 'Sala 3 (Unidade Central)', '15:30', 'RESERVED'], ['Ana Beatriz', 'Fisioterapia', 'Thiago Prado', 'Sala 4 (Unidade Central)', '17:00', 'CONFIRMED'], ['Pedro Souza', 'Psicologia', 'Camila Duarte', 'Sala 1 (Unidade Sul)', '11:00', 'SCHEDULED'], ['Maria Fernandes', 'Fonoaudiologia', 'Rafael Nogueira', 'Sala 2 (Unidade Sul)', '14:45', 'CONFIRMED'], ['Lucas Andrade', 'Terapia ocupacional', 'Beatriz Alves', 'Sala 3 (Unidade Sul)', '18:30', 'PENDING'],
  ['Valentina Alves', 'Psicologia', 'Camila Duarte', 'Sala 1 (Unidade Central)', '10:15', 'RESERVED'], ['Arthur Mendes', 'Fonoaudiologia', 'Rafael Nogueira', 'Sala 2 (Unidade Central)', '11:45', 'PENDING'], ['Manuela Castro', 'Fisioterapia', 'Thiago Prado', 'Sala 4 (Unidade Central)', '08:45', 'RESERVED'], ['Nicolas Freitas', 'Terapia ocupacional', 'Beatriz Alves', 'Sala 3 (Unidade Sul)', '13:15', 'PENDING'], ['Lívia Martins', 'Psicologia', 'Camila Duarte', 'Sala 1 (Unidade Sul)', '17:00', 'RESERVED'], ['Ravi Oliveira', 'Fonoaudiologia', 'Rafael Nogueira', 'Sala 2 (Unidade Sul)', '15:30', 'PENDING'], ['Clara Monteiro', 'Fisioterapia', 'Thiago Prado', 'Sala 4 (Unidade Sul)', '09:30', 'RESERVED'], ['Bento Cardoso', 'Psicologia', 'Camila Duarte', 'Sala 1 (Unidade Central)', '18:30', 'PENDING'], ['Yasmin Ribeiro', 'Terapia ocupacional', 'Beatriz Alves', 'Sala 3 (Unidade Central)', '14:45', 'RESERVED'], ['Cecília Ramos', 'Psicologia', 'Camila Duarte', 'Sala 1 (Unidade Central)', '12:30', 'CANCELED'], ['Gustavo Martins', 'Fonoaudiologia', 'Rafael Nogueira', 'Sala 2 (Unidade Sul)', '17:45', 'CANCELADO'], ['Marina Lopes', 'Fisioterapia', 'Thiago Prado', 'Sala 4 (Unidade Central)', '19:15', 'CANCELED'], ['', '', '', 'Sala 4 (Unidade Central)', '12:30', 'BLOCKED'], ['', '', '', 'Sala 2 (Unidade Sul)', '12:30', 'BLOQUEADO'], ['', '', '', 'Sala 3 (Unidade Central)', '08:00', 'BLOCKED'],
] as const;
const createMockAppointments = (weekStart: dayjs.Dayjs): TeaAgendaItem[] => {
  const occurrencesByRoom = new Map<string, number>();
  const appointments = Array.from({ length: 3 }, (_, round) => MOCK_PATIENTS.map((item, index) => {
    const occurrence = occurrencesByRoom.get(item[3]) || 0;
    occurrencesByRoom.set(item[3], occurrence + 1);
    return { id: `tea-demo-${round}-${index}`, patientName: item[0], specialty: item[1], doctorName: item[2], roomName: item[3], time: SLOTS[(Math.floor(occurrence / 5) * 4 + index % 4) % SLOTS.length], status: item[5], date: weekStart.add(occurrence % 5, 'day').format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' as const };
  })).flat();
  const simultaneousAppointments: TeaAgendaItem[] = [
    { id: 'tea-demo-simultaneous-1', patientName: 'Mariana Azevedo', specialty: 'Psicologia', doctorName: 'Camila Duarte', roomName: 'Sala 1 (Unidade Central)', time: '09:30', status: 'SCHEDULED', date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
    { id: 'tea-demo-simultaneous-2', patientName: 'Thiago Mendonça', specialty: 'Terapia ocupacional', doctorName: 'Beatriz Alves', roomName: 'Sala 1 (Unidade Central)', time: '09:30', status: 'RESERVED', date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
    { id: 'tea-demo-simultaneous-4', patientName: 'Júlia Martins', specialty: 'Fonoaudiologia', doctorName: 'Rafael Nogueira', roomName: 'Sala 1 (Unidade Central)', time: '09:30', status: 'PENDING', date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
    { id: 'tea-demo-simultaneous-5', patientName: 'Pedro Henrique', specialty: 'Fisioterapia', doctorName: 'Thiago Prado', roomName: 'Sala 1 (Unidade Central)', time: '09:30', status: 'CANCELED', date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
    { id: 'tea-demo-simultaneous-6', patientName: 'Luiza Fernandes', specialty: 'Psicologia', doctorName: 'Camila Duarte', roomName: 'Sala 1 (Unidade Central)', time: '09:30', status: 'SCHEDULED', date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
    { id: 'tea-demo-simultaneous-7', patientName: 'Caio Almeida', specialty: 'Terapia ocupacional', doctorName: 'Beatriz Alves', roomName: 'Sala 1 (Unidade Central)', time: '09:30', status: 'RESERVED', date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
    { id: 'tea-demo-simultaneous-8', patientName: 'Bianca Costa', specialty: 'Fisioterapia', doctorName: 'Thiago Prado', roomName: 'Sala 1 (Unidade Central)', time: '09:30', status: 'SCHEDULED', date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
    { id: 'tea-demo-simultaneous-9', patientName: 'Rafael Santos', specialty: 'Psicologia', doctorName: 'Camila Duarte', roomName: 'Sala 1 (Unidade Central)', time: '09:30', status: 'PENDING', date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
    { id: 'tea-demo-simultaneous-10', patientName: 'Aline Souza', specialty: 'Fonoaudiologia', doctorName: 'Rafael Nogueira', roomName: 'Sala 1 (Unidade Central)', time: '09:30', status: 'RESERVED', date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
    { id: 'tea-demo-simultaneous-11', patientName: 'Diego Lima', specialty: 'Terapia ocupacional', doctorName: 'Beatriz Alves', roomName: 'Sala 1 (Unidade Central)', time: '09:30', status: 'CANCELED', date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
    { id: 'tea-demo-simultaneous-3', patientName: 'Larissa Freitas', specialty: 'Fonoaudiologia', doctorName: 'Rafael Nogueira', roomName: 'Sala 1 (Unidade Central)', time: '14:00', status: 'PENDING', date: weekStart.add(2, 'day').format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' },
  ];
  const mixedRoomSlots: TeaAgendaItem[] = [
    { time: '08:00', patients: [['Marina Alves', 'Psicologia', 'Camila Duarte', 'SCHEDULED'], ['Otávio Ribeiro', 'Fisioterapia', 'Thiago Prado', 'RESERVED']] },
    { time: '10:15', patients: [['Beatriz Moura', 'Fonoaudiologia', 'Rafael Nogueira', 'SCHEDULED'], ['Henrique Dias', 'Terapia ocupacional', 'Beatriz Alves', 'PENDING'], ['Lorena Campos', 'Psicologia', 'Camila Duarte', 'RESERVED']] },
    { time: '11:45', patients: [['Alice Martins', 'Fisioterapia', 'Thiago Prado', 'SCHEDULED'], ['Guilherme Reis', 'Psicologia', 'Camila Duarte', 'PENDING'], ['Nathalia Souza', 'Fonoaudiologia', 'Rafael Nogueira', 'RESERVED'], ['Samuel Costa', 'Terapia ocupacional', 'Beatriz Alves', 'CANCELED'], ['Isadora Lima', 'Psicologia', 'Camila Duarte', 'SCHEDULED']] },
    { time: '13:15', patients: [['Rafaela Nunes', 'Fonoaudiologia', 'Rafael Nogueira', 'SCHEDULED'], ['Matheus Freire', 'Fisioterapia', 'Thiago Prado', 'RESERVED'], ['Elisa Rocha', 'Psicologia', 'Camila Duarte', 'PENDING'], ['Bruno Martins', 'Terapia ocupacional', 'Beatriz Alves', 'SCHEDULED'], ['Sabrina Lopes', 'Fonoaudiologia', 'Rafael Nogueira', 'RESERVED'], ['Vitor Hugo', 'Psicologia', 'Camila Duarte', 'CANCELED']] },
    { time: '15:30', patients: [['Amanda Castro', 'Psicologia', 'Camila Duarte', 'SCHEDULED'], ['João Pedro', 'Fisioterapia', 'Thiago Prado', 'SCHEDULED'], ['Carolina Mendes', 'Fonoaudiologia', 'Rafael Nogueira', 'RESERVED'], ['Felipe Araujo', 'Terapia ocupacional', 'Beatriz Alves', 'PENDING'], ['Manuela Alves', 'Psicologia', 'Camila Duarte', 'SCHEDULED'], ['Diego Martins', 'Fonoaudiologia', 'Rafael Nogueira', 'RESERVED'], ['Yasmin Oliveira', 'Fisioterapia', 'Thiago Prado', 'CANCELED']] },
  ].flatMap(({ time, patients }, groupIndex) => patients.map(([patientName, specialty, doctorName, status], patientIndex) => ({ id: `tea-demo-mixed-${groupIndex}-${patientIndex}`, patientName, specialty, doctorName, roomName: 'Sala 1 (Unidade Central)', time, status, date: weekStart.format('YYYY-MM-DD'), type: 'ATENDIMENTO TEA', source: 'APPOINTMENT' as const })));
  return [...appointments, ...simultaneousAppointments, ...mixedRoomSlots];
};
const toWeekStartMonday = (value: dayjs.Dayjs) => value.subtract((value.day() + 6) % 7, 'day').startOf('day');
const roomUnit = (room: string) => room.match(/\(([^)]+)\)/)?.[1] || 'Unidade não informada';
const roomDisplay = (room: string) => ({ name: room.replace(/\s*\([^)]*\)\s*$/, '').trim() || room, unit: roomUnit(room) });
const patientInitials = (name?: string) => { const parts = String(name || '').trim().split(/\s+/).filter(Boolean); if (!parts.length) return ''; return `${parts[0][0]}${(parts.length > 1 ? parts[parts.length - 1][0] : parts[0][1] || '')}`.toUpperCase(); };
const isCanceledStatus = (value?: string) => ['CANCELED', 'CANCELADO'].includes(String(value || '').toUpperCase());
const statusLabel = (value?: string) => { const v = String(value || '').toUpperCase(); if (['CANCELED', 'CANCELADO'].includes(v)) return 'Cancelado'; if (['BLOCKED', 'BLOQUEADO'].includes(v)) return 'Bloqueado'; if (['RESERVED', 'RESERVADO'].includes(v)) return 'Reservado'; if (['PENDING', 'PENDENTE'].includes(v)) return 'Pendente'; return 'Agendado'; };
const statusColor = (value?: string) => { const v = String(value || '').toUpperCase(); if (['CANCELED', 'CANCELADO'].includes(v)) return 'red'; if (['BLOCKED', 'BLOQUEADO'].includes(v)) return 'orange'; if (['RESERVED', 'RESERVADO'].includes(v)) return 'violet'; if (['PENDING', 'PENDENTE'].includes(v)) return 'yellow'; return 'teal'; };
const statusSurface = (value?: string) => { const v = String(value || '').toUpperCase(); if (['CANCELED', 'CANCELADO'].includes(v)) return { background: 'rgba(185, 28, 28, .38)', border: 'rgba(239, 68, 68, .9)' }; if (['BLOCKED', 'BLOQUEADO'].includes(v)) return { background: 'rgba(217, 119, 6, .38)', border: 'rgba(245, 158, 11, .9)' }; if (['RESERVED', 'RESERVADO'].includes(v)) return { background: 'rgba(139, 92, 246, .24)', border: 'rgba(139, 92, 246, .7)' }; if (['PENDING', 'PENDENTE'].includes(v)) return { background: 'rgba(234, 179, 8, .2)', border: 'rgba(234, 179, 8, .7)' }; return { background: 'rgba(72, 187, 155, .28)', border: 'rgba(72, 187, 155, .7)' }; };

function AppointmentCard({ item, compact = false, detailLevel = 'minimal', patientOnly = false, showInitials = false, hideContent = false }: { item: TeaAgendaItem; compact?: boolean; detailLevel?: AgendaCardDetailLevel; patientOnly?: boolean; showInitials?: boolean; hideContent?: boolean }) {
  const tooltipContent = compact && detailLevel === 'minimal' ? <Box p={2} style={{ minWidth: 220 }}>
    <Group justify="space-between" gap="sm" mb={6} wrap="nowrap"><Text size="sm" fw={700}>{item.patientName}</Text><Badge size="sm" variant="light" color={statusColor(item.status)}>{item.time}</Badge></Group>
    <Text size="xs" c="dimmed" mb={4}>{item.roomName || 'Sala não vinculada'}</Text>
    <Stack gap={3}><Text size="sm" fw={600}>{item.specialty || 'Terapia não informada'}</Text><Text size="xs">Terapeuta: {item.doctorName || 'Não informado'}</Text><Text size="xs" c="dimmed">Status: {statusLabel(item.status)}</Text></Stack>
  </Box> : patientOnly ? <Box p={2} style={{ minWidth: 220 }}>
    <Group justify="space-between" gap="sm" mb={6} wrap="nowrap"><Text size="sm" fw={700}>{item.patientName || 'Detalhes do atendimento'}</Text><Badge size="sm" variant="light" color={statusColor(item.status)}>{item.time}</Badge></Group>
    <Text size="xs" c="dimmed" mb={4}>{item.roomName || 'Sala não vinculada'}</Text>
    <Stack gap={3}><Text size="sm" fw={600}>{item.specialty || 'Terapia não informada'}</Text><Text size="xs">Terapeuta: {item.doctorName || 'Não informado'}</Text><Text size="xs" c="dimmed">Status: {statusLabel(item.status)}</Text></Stack>
  </Box> : compact && detailLevel === 'summary' ? <Box p={2} style={{ minWidth: 210 }}><Group justify="space-between" gap="sm" mb={6} wrap="nowrap"><Text size="sm" fw={700}>Detalhes do atendimento</Text><Badge size="sm" variant="light" color={statusColor(item.status)}>{item.time}</Badge></Group><Text size="xs" c="dimmed" mb={4}>{item.roomName || 'Sala não vinculada'}</Text><Text size="xs">Terapeuta: {item.doctorName || 'Não informado'}</Text><Text size="xs" c="dimmed">Status: {statusLabel(item.status)}</Text></Box> : <Box p={2} style={{ minWidth: 190 }}><Group justify="space-between" gap="sm" mb={6} wrap="nowrap"><Text size="sm" fw={700}>Detalhes do atendimento</Text><Badge size="sm" variant="light" color={statusColor(item.status)}>{item.time}</Badge></Group><Text size="xs" c="dimmed">{item.roomName || 'Sala não vinculada'}</Text><Text size="xs" c="dimmed">Status: {statusLabel(item.status)}</Text></Box>;
  return <Tooltip label={tooltipContent} withArrow position="top" offset={8} openDelay={180} multiline styles={{ tooltip: { background: 'var(--mantine-color-dark-7)', color: 'var(--mantine-color-white)', border: '1px solid var(--mantine-color-default-border)', boxShadow: '0 10px 28px rgba(0, 0, 0, 0.28)', padding: 12 }, arrow: { background: 'var(--mantine-color-dark-7)', borderColor: 'var(--mantine-color-default-border)' } }}>
    {compact
      ? detailLevel !== 'minimal' ? <Box aria-label={`${item.patientName || statusLabel(item.status)} · ${item.time}`} style={{ display: 'flex', flexDirection: showInitials ? 'row' : 'column', alignItems: showInitials || hideContent ? 'center' : 'flex-start', justifyContent: showInitials || hideContent ? 'center' : 'flex-start', width: '100%', height: '100%', boxSizing: 'border-box', padding: hideContent ? 0 : 3, borderRadius: 3, border: `1px solid ${statusSurface(item.status).border}`, background: statusSurface(item.status).background, overflow: 'hidden' }}>{!hideContent && <><Text fw={700} size="xs" ta={showInitials ? 'center' : undefined} truncate>{showInitials ? patientInitials(item.patientName) : (item.patientName || statusLabel(item.status))}</Text>{!patientOnly && <Text size="xs" c="dimmed" truncate>{item.specialty || 'Terapia não informada'}</Text>}{!patientOnly && detailLevel === 'full' && <Text size="xs" c="dimmed" truncate>{item.doctorName || 'Terapeuta não informado'}</Text>}</>}</Box> : <Box aria-label={`${item.patientName || statusLabel(item.status)} · ${item.time}`} style={{ width: '100%', height: '100%', borderRadius: 3, border: `1px solid ${statusSurface(item.status).border}`, background: statusSurface(item.status).background }} />
      : <Paper p={4} radius="sm" withBorder style={{ ...statusSurface(item.status), width: '100%', height: '100%', alignSelf: 'stretch', boxSizing: 'border-box', minWidth: 0, marginBottom: 0 }}><Text fw={700} size="xs" truncate>{item.patientName || statusLabel(item.status)}</Text>{item.patientName ? <><Text size="xs" c="dimmed" style={{ fontSize: 11 }} truncate>{item.specialty || 'Terapia não informada'}</Text><Text size="xs" c="dimmed" style={{ fontSize: 11 }} truncate>{item.doctorName || 'Terapeuta não informado'}</Text></> : <Text size="xs" c="dimmed" style={{ fontSize: 11 }} truncate>Indisponível para atendimento</Text>}</Paper>}
  </Tooltip>;
}

export function TeaAgendaSemanal() {
  const navigate = useNavigate();
  const isMobile = useMediaQuery('(max-width: 799px)');
  const { ref: scheduleRef, width: scheduleWidth } = useElementSize();
  const { data: apiAppointments = [], isLoading: loading, error } = useTeaWeeklyAgendaQuery();
  const [mode, setMode] = useState<'day' | 'week'>('week');
  const [selectedDate, setSelectedDate] = useState(() => dayjs().format('YYYY-MM-DD'));
  const [weekStart, setWeekStart] = useState(() => toWeekStartMonday(dayjs()));
  const [search, setSearch] = useState('');
  const [unitFilter, setUnitFilter] = useState<string[]>([]);
  const [specialtyFilter, setSpecialtyFilter] = useState<string[]>([]);
  const [doctorFilter, setDoctorFilter] = useState<string[]>([]);
  const [roomFilter, setRoomFilter] = useState<string[]>([]);
  const [patientFilter, setPatientFilter] = useState<string[]>([]);
  const isDemo = import.meta.env.DEV && !loading && apiAppointments.length === 0;
  const allAppointments = useMemo(() => isDemo ? createMockAppointments(weekStart) : apiAppointments, [apiAppointments, isDemo, weekStart]);
  const visibleAppointments = useMemo(() => allAppointments.filter((item) => !isCanceledStatus(item.status)), [allAppointments]);
  const days = useMemo(() => Array.from({ length: 5 }, (_, index) => weekStart.add(index, 'day')), [weekStart]);
  const options = useMemo(() => ({ units: Array.from(new Set(visibleAppointments.map((item) => roomUnit(item.roomName)).filter(Boolean))).sort(), specialties: Array.from(new Set(visibleAppointments.map((item) => item.specialty).filter(Boolean))).sort(), doctors: Array.from(new Set(visibleAppointments.map((item) => item.doctorName).filter(Boolean))).sort(), rooms: Array.from(new Set(visibleAppointments.map((item) => item.roomName).filter(Boolean))).sort(), patients: Array.from(new Set(visibleAppointments.map((item) => item.patientName).filter(Boolean))).sort() }), [visibleAppointments]);
  const filtered = useMemo(() => visibleAppointments.filter((item) => { const query = search.trim().toLowerCase(); const matchesQuery = !query || [item.patientName, item.specialty, item.doctorName, item.roomName, item.time].some((value) => String(value || '').toLowerCase().includes(query)); return matchesQuery && (!unitFilter.length || unitFilter.includes(roomUnit(item.roomName))) && (!specialtyFilter.length || specialtyFilter.includes(item.specialty)) && (!doctorFilter.length || doctorFilter.includes(item.doctorName)) && (!roomFilter.length || roomFilter.includes(item.roomName)) && (!patientFilter.length || patientFilter.includes(item.patientName)); }), [visibleAppointments, search, unitFilter, specialtyFilter, doctorFilter, roomFilter, patientFilter]);
  const bySlot = useMemo(() => { const map = new Map<string, TeaAgendaItem[]>(); filtered.forEach((item) => { const key = `${item.date}#${item.time}`; map.set(key, [...(map.get(key) || []), item]); }); return map; }, [filtered]);
  const getScheduleCellWidth = () => { const columnCount = mode === 'day' ? Math.max(rooms.length, 1) : days.length * Math.max(rooms.length, 1); const timeColumnWidth = mode === 'day' ? 110 : 90; const fallbackWidth = mode === 'day' ? Math.max(760, 132 + rooms.length * 142) : Math.max(1120, 90 + days.length * Math.max(rooms.length, 1) * 112); const gridWidth = Math.max(scheduleWidth, fallbackWidth); return Math.max(40, (gridWidth - timeColumnWidth - columnCount * 6) / columnCount); };
  const renderSlotItems = (items: TeaAgendaItem[] | undefined, compact = false, detailLevel: AgendaCardDetailLevel = 'minimal') => {
    if (!items?.length) return compact ? null : <Text size="xs" c="dimmed" ta="center" py={14}>Disponível</Text>;
    const cellWidth = getScheduleCellWidth();
    if (!compact) {
      if (items.length === 1 && !items[0].patientName) return <AppointmentCard key={items[0].id} item={items[0]} />;
      if (items.length === 1) {
        const presentation = resolveAgendaCardPresentation(cellWidth, 1, 'full');
        if (presentation.detailLevel === 'full' && !presentation.patientOnly) return <AppointmentCard key={items[0].id} item={items[0]} />;
        return <Box style={{ width: '100%', height: '100%' }}><AppointmentCard key={items[0].id} item={items[0]} compact {...presentation} /></Box>;
      }
      const presentation = resolveAgendaCardPresentation(cellWidth, items.length, 'full');
      return <Box style={{ display: 'grid', gridTemplateColumns: `repeat(${items.length}, minmax(0, 1fr))`, gap: 2, width: '100%', height: '100%' }}>{items.map((item) => <AppointmentCard key={item.id} item={item} compact {...presentation} />)}</Box>;
    }
    const presentation = resolveAgendaCardPresentation(cellWidth, items.length, detailLevel);
    return <Box style={{ display: 'grid', gridTemplateColumns: `repeat(${items.length}, minmax(0, 1fr))`, gap: 2, width: '100%', height: '100%' }}>{items.map((item) => <AppointmentCard key={item.id} item={item} compact {...presentation} />)}</Box>;
  };
  const rooms = useMemo(() => Array.from(new Set(filtered.map((item) => item.roomName).filter(Boolean))).sort(), [filtered]);
  const weeklyDetailLevel: AgendaCardDetailLevel = 'full';
  const showWeeklyCardDetails = getScheduleCellWidth() >= AGENDA_CARD_DENSITY.summaryMinWidth;
  const weekTotal = filtered.filter((item) => item.date >= weekStart.format('YYYY-MM-DD') && item.date <= weekStart.add(4, 'day').format('YYYY-MM-DD')).length;
  const dateLabel = dayjs(selectedDate).locale('pt-br').format('dddd, DD [de] MMMM [de] YYYY');
  useEffect(() => { dayjs.locale('pt-br'); }, []);
  useEffect(() => { document.body.dataset.teaAgendaMode = mode; document.body.dataset.teaAgendaDensity = showWeeklyCardDetails ? 'expanded' : 'compact'; return () => { delete document.body.dataset.teaAgendaMode; delete document.body.dataset.teaAgendaDensity; }; }, [mode, showWeeklyCardDetails]);
  useEffect(() => { if (error) showNotification({ title: 'Erro', message: resolveApiErrorMessage(error as any, 'Erro ao carregar agenda semanal TEA'), color: 'red' }); }, [error]);
  const moveDay = (amount: number) => { const next = dayjs(selectedDate).add(amount, 'day'); setSelectedDate(next.format('YYYY-MM-DD')); setWeekStart(toWeekStartMonday(next)); };
  const setToday = () => { const today = dayjs(); setSelectedDate(today.format('YYYY-MM-DD')); setWeekStart(toWeekStartMonday(today)); };

  return <Box bg="var(--mantine-color-body)" style={{ minHeight: '100vh' }}><Header /><Box p={isMobile ? 'sm' : 'xl'} maw={1600} mx="auto" w="100%">
    <Group mb="lg" gap="md"><Button variant="default" leftSection={<ChevronLeft size={18} />} onClick={() => navigate('/tea')}>Voltar</Button><Box><Text fw={700} size="lg">Agenda de terapias TEA</Text><Text size="sm" c="dimmed">Acompanhe os atendimentos por paciente, terapeuta e sala</Text></Box></Group>
      <Paper p={isMobile ? 'sm' : 'md'} withBorder radius="md"><Stack gap="md"><style>{`body[data-tea-agenda-mode="week"] div[style*="grid-template-columns"][style*="min-height"] { min-height: 36px !important; height: 36px !important; gap: 2px !important; margin-bottom: 2px !important; } body[data-tea-agenda-mode="week"] div[style*="grid-template-columns"][style*="min-height"] > div:not(:first-child) { min-height: 36px !important; height: 36px !important; box-sizing: border-box !important; padding: 0 !important; border: 1px solid var(--mantine-color-default-border) !important; border-radius: 3px !important; background: transparent !important; } body[data-tea-agenda-mode="week"][data-tea-agenda-density="expanded"] div[style*="grid-template-columns"][style*="min-height"], body[data-tea-agenda-mode="week"][data-tea-agenda-density="expanded"] div[style*="grid-template-columns"][style*="min-height"] > div:not(:first-child) { min-height: 56px !important; height: 56px !important; }`}</style>
      <Group justify="flex-end" align="center" wrap="wrap"><Group gap={6}><Button size="xs" variant={mode === 'day' ? 'filled' : 'default'} onClick={() => setMode('day')}>Dia</Button><Button size="xs" variant={mode === 'week' ? 'filled' : 'default'} onClick={() => setMode('week')}>Semana</Button></Group></Group>
      <Group grow align="flex-start" wrap="wrap"><FloatingMultiSelect label="Unidade" placeholder="Todas as unidades" data={options.units} value={unitFilter} onChange={setUnitFilter} searchable clearable /><FloatingMultiSelect label="Especialidade" placeholder="Todas as especialidades" data={options.specialties} value={specialtyFilter} onChange={setSpecialtyFilter} searchable clearable /><FloatingMultiSelect label="Terapeuta" placeholder="Todos os terapeutas" data={options.doctors} value={doctorFilter} onChange={setDoctorFilter} searchable clearable /><FloatingMultiSelect label="Sala" placeholder="Todas as salas" data={options.rooms} value={roomFilter} onChange={setRoomFilter} searchable clearable /><FloatingMultiSelect label="Paciente" placeholder="Todos os pacientes" data={options.patients} value={patientFilter} onChange={setPatientFilter} searchable clearable /></Group>
      <Group justify="space-between" align="center" wrap="wrap"><Paper withBorder radius="md" p={4} style={{ background: 'rgba(59, 130, 246, 0.04)' }}><Group gap="xs" wrap="wrap">{mode === 'day' ? <><Button size="xs" variant="default" onClick={() => moveDay(-1)} leftSection={<ChevronLeft size={14} />}>Dia anterior</Button><Button size="xs" variant="light" onClick={setToday}>Hoje</Button><Button size="xs" variant="default" onClick={() => moveDay(1)} rightSection={<ChevronRight size={14} />}>Próximo dia</Button><Divider orientation="vertical" my={4} /><Group gap={2} wrap="nowrap">{WEEKDAY_LABELS.map((label, index) => { const day = weekStart.add(index, 'day'); const selected = selectedDate === day.format('YYYY-MM-DD'); return <Button key={label} size="xs" variant={selected ? 'filled' : 'subtle'} color={selected ? 'blue' : undefined} px={10} onClick={() => setSelectedDate(day.format('YYYY-MM-DD'))} aria-label={`Selecionar ${label}`}>{label}</Button>; })}</Group></> : <><Button size="xs" variant="default" onClick={() => setWeekStart((prev) => prev.subtract(7, 'day'))} leftSection={<ChevronLeft size={14} />}>Semana anterior</Button><Button size="xs" variant="light" onClick={setToday}>Hoje</Button><Button size="xs" variant="default" onClick={() => setWeekStart((prev) => prev.add(7, 'day'))} rightSection={<ChevronRight size={14} />}>Próxima semana</Button></>}</Group></Paper><Group gap="xs" align="center"><FloatingInput label={null} placeholder="Buscar por paciente, sala, terapeuta ou horário" value={search} onChange={(event) => setSearch(event.currentTarget.value)} rightSection={<Search size={17} />} /><Text size="sm" c="dimmed">{mode === 'day' ? dateLabel : `${weekStart.format('DD/MM/YYYY')} até ${weekStart.add(4, 'day').format('DD/MM/YYYY')}`}</Text></Group></Group>
      <Box style={{ display: 'grid', gridTemplateColumns: isMobile ? 'repeat(2, minmax(0, 1fr))' : 'repeat(4, minmax(0, 1fr))', gap: 10 }}><Paper withBorder radius="md" p="sm"><Text size="xl" fw={700}>{weekTotal}</Text><Text size="sm" fw={600}>Atendimentos</Text><Text size="xs" c="dimmed">na semana exibida</Text></Paper><Paper withBorder radius="md" p="sm"><Text size="xl" fw={700}>{new Set(filtered.map((item) => item.patientName)).size}</Text><Text size="sm" fw={600}>Pacientes</Text><Text size="xs" c="dimmed">com atendimento</Text></Paper><Paper withBorder radius="md" p="sm"><Text size="xl" fw={700}>{rooms.length}</Text><Text size="sm" fw={600}>Salas</Text><Text size="xs" c="dimmed">em uso na visão</Text></Paper><Paper withBorder radius="md" p="sm" style={{ borderColor: 'rgba(72, 187, 155, 0.6)', background: 'rgba(72, 187, 155, 0.12)' }}><Text size="xl" fw={700}>{filtered.filter((item) => !['CANCELED', 'CANCELADO'].includes(String(item.status).toUpperCase())).length}</Text><Text size="sm" fw={600}>Agendados</Text><Text size="xs" c="dimmed">atendimentos ativos</Text></Paper></Box>
      <Group gap="lg" mb={-4}>{[['teal', 'Agendado'], ['violet', 'Reservado'], ['yellow', 'Pendente'], ['orange', 'Bloqueado']] .map(([color, label]) => <Group key={color} gap={6}><Box w={12} h={12} style={{ borderRadius: 3, background: `var(--mantine-color-${color}-5)`, border: `1px solid var(--mantine-color-${color}-6)` }} /><Text size="xs" c="dimmed">{label}</Text></Group>)}</Group>
      {loading ? <Box style={{ display: 'grid', gridTemplateColumns: 'repeat(5, minmax(180px, 1fr))', gap: 8 }}>{Array.from({ length: 5 }).map((_, index) => <Skeleton key={index} height={500} radius="md" />)}</Box> : <Box ref={scheduleRef} style={{ overflowX: 'auto', border: '1px solid var(--mantine-color-default-border)', borderRadius: 10 }}>
        {mode === 'day' ? <Box style={{ minWidth: Math.max(760, 132 + rooms.length * 142) }}><Box style={{ display: 'grid', gridTemplateColumns: `110px repeat(${Math.max(rooms.length, 1)}, minmax(128px, 1fr))`, gap: 6, marginBottom: 8, background: 'rgba(59, 130, 246, 0.08)', borderRadius: 8 }}><Box p="sm"><Text size="xs" fw={700} c="dimmed">HORÁRIO</Text><Text size="xs" c="dimmed">{dayjs(selectedDate).format('DD/MM')}</Text></Box>{rooms.map((room) => <Box key={room} p="sm" style={{ border: '1px solid var(--mantine-color-indigo-5)', borderRadius: 6 }}><Text fw={700} size="sm" truncate>{roomDisplay(room).name}</Text><Text size="xs" c="dimmed">{roomDisplay(room).unit}</Text></Box>)}</Box>{SLOTS.map((slot) => <Box key={slot} style={{ display: 'grid', gridTemplateColumns: `110px repeat(${Math.max(rooms.length, 1)}, minmax(128px, 1fr))`, minHeight: 0, gap: 6, marginBottom: 6 }}><Box p="sm" style={{ display: 'flex', alignItems: 'center', borderRight: '1px solid var(--mantine-color-default-border)' }}><Group gap={5} wrap="nowrap"><Clock3 size={13} /><Text size="xs" c="dimmed">{slot}</Text></Group></Box>{rooms.length ? rooms.map((room) => <Box key={`${slot}-${room}`} p={0} style={{ border: '1px solid var(--mantine-color-default-border)', borderRadius: 6, background: 'var(--mantine-color-body)' }}>{renderSlotItems((bySlot.get(`${selectedDate}#${slot}`) || []).filter((item) => item.roomName === room))}</Box>) : <Box p="sm" style={{ border: '1px solid var(--mantine-color-default-border)', borderRadius: 6 }}><Text size="xs" c="dimmed">Nenhum resultado</Text></Box>}</Box>)}</Box> : <Box style={{ minWidth: Math.max(1120, 90 + days.length * Math.max(rooms.length, 1) * 112) }}><Box style={{ display: 'grid', gridTemplateColumns: `90px repeat(${days.length * Math.max(rooms.length, 1)}, minmax(100px, 1fr))`, gap: 6, marginBottom: 6 }}><Box p="sm" style={{ background: 'rgba(59, 130, 246, 0.08)', borderRadius: 8 }}><Text size="xs" fw={700} c="dimmed">HORÁRIO</Text></Box>{days.map((day, index) => <Box key={day.format('YYYY-MM-DD')} p="sm" style={{ gridColumn: `span ${Math.max(rooms.length, 1)}`, border: '1px solid var(--mantine-color-indigo-5)', borderRadius: 8, background: 'rgba(59, 130, 246, 0.08)' }}><Text fw={700} size="sm">{WEEKDAY_LABELS[index]} · {day.format('DD/MM')}</Text><Text size="xs" c="dimmed">{filtered.filter((item) => item.date === day.format('YYYY-MM-DD')).length} atendimento(s)</Text></Box>)}</Box><Box style={{ display: 'grid', gridTemplateColumns: `90px repeat(${days.length * Math.max(rooms.length, 1)}, minmax(100px, 1fr))`, gap: 6, marginBottom: 8 }}><Box />{days.flatMap((day) => rooms.length ? rooms.map((room) => <Tooltip key={`${day.format('YYYY-MM-DD')}-${room}`} label={room} withArrow openDelay={180} multiline styles={{ tooltip: { background: 'var(--mantine-color-dark-7)', color: 'var(--mantine-color-white)', border: '1px solid var(--mantine-color-default-border)', boxShadow: '0 10px 28px rgba(0, 0, 0, 0.28)' }, arrow: { background: 'var(--mantine-color-dark-7)', borderColor: 'var(--mantine-color-default-border)' } }}><Box p={5} style={{ minWidth: 0, borderLeft: '1px solid var(--mantine-color-default-border)', borderRight: '1px solid var(--mantine-color-default-border)' }}><Text size="xs" c="dimmed" ta="center">{roomDisplay(room).name}</Text><Text size="xs" c="dimmed" ta="center">{roomDisplay(room).unit}</Text></Box></Tooltip>) : [<Box key={`${day.format('YYYY-MM-DD')}-empty`} />])}</Box>{SLOTS.map((slot) => <Box key={slot} style={{ display: 'grid', gridTemplateColumns: `90px repeat(${days.length * Math.max(rooms.length, 1)}, minmax(100px, 1fr))`, minHeight: 76, gap: 6, marginBottom: 6 }}><Box p="sm" style={{ display: 'flex', alignItems: 'center', borderRight: '1px solid var(--mantine-color-default-border)' }}><Text size="xs" c="dimmed">{slot}</Text></Box>{days.flatMap((day) => rooms.length ? rooms.map((room) => <Box key={`${day.format('YYYY-MM-DD')}-${slot}-${room}`} p={5} style={{ border: '1px solid var(--mantine-color-default-border)', borderRadius: 6, background: 'var(--mantine-color-body)' }}>{renderSlotItems((bySlot.get(`${day.format('YYYY-MM-DD')}#${slot}`) || []).filter((item) => item.roomName === room), true, weeklyDetailLevel)}</Box>) : [<Box key={`${day.format('YYYY-MM-DD')}-${slot}-empty`} p="sm"><Text size="xs" c="dimmed">Nenhum resultado</Text></Box>])}</Box>)}</Box>}
      </Box>}
      {!loading && !filtered.length && <Text ta="center" c="dimmed" py="xl">Nenhum atendimento encontrado com os filtros atuais.</Text>}
      {!loading && <Text size="xs" c="dimmed">Passe o mouse sobre um atendimento para consultar os detalhes.</Text>}
    </Stack></Paper>
  </Box></Box>;
}
